apiVersion: v1
kind: Service
metadata:
  name: final-project-service
  namespace: ivolve
spec:
  selector:
    app: final-project
  ports:
  - port: 80
    targetPort: 5000
  type: LoadBalancer